NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_SITE_URL=https://tekliflercepte.com

# Must be the exact same value as apps/api/.env and apps/panel/.env — this
# app verifies the same session JWT the API issues.
JWT_SECRET=change-me
